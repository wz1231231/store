spring:
  datasource:
    #        type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/store?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
user:
  address:
    max-count:
          20

#  web:
#    resources:
#      static-locations: classpath:/static,file:D:images/upload
mybatis:
  type-aliases-package: com.wz.store.entity
  mapper-locations: classpath:mapper/*.xml
server:

